# Polymarket Trading Bot — PAF-001
# Python 3.10+
# For deterministic installs: pip install -r requirements.txt

# ── Polymarket CLOB ──────────────────────────────────────────────────────────
py-clob-client>=0.34.6

# ── HTTP / WebSocket ─────────────────────────────────────────────────────────
aiohttp>=3.9.0
websockets>=12.0
httpx[http2]>=0.27.0     # requis par py-clob-client (HTTP/2)

# ── Données / Calcul ─────────────────────────────────────────────────────────
numpy>=1.26.0
scipy>=1.12.0
scikit-learn>=1.4.0
pandas>=2.2.0

# ── NLP — Reference Class Engine (optionnel, fallback keyword si absent) ────
sentence-transformers>=2.7.0
torch>=2.0.0

# ── SSL ──────────────────────────────────────────────────────────────────────
certifi>=2024.2.2

# ── XML parsing ──────────────────────────────────────────────────────────────
lxml>=5.1.0

# ── Config ───────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0

# ── Dev / Test (install with: pip install -r requirements-dev.txt) ───────────
# pytest, pytest-cov, pytest-asyncio, ruff, mypy, bandit, pip-audit

# ── REMOVED: requests (replaced by aiohttp), feedparser (unused),
#    asyncio-throttle (unused)
